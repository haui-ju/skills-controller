# Agents — suit design

Aquí viven prompts de subagentes del skill `design` (mismo patrón que skill-creator).

Por ahora no hay agentes definidos. Cuando se agreguen:

```text
agents/
  critic.md      # ejemplo futuro
  polish.md
```

El `SKILL.md` del skill debe indicar cuándo leer o spawnear cada agente.
